# Graphics -- Run artifacts

Some tagged releases come with these run artifacts that show results for various schedulers.

In principle, they can be regenerated by backing up to that tag and re-running the 
observation "trace" (plotting) code. But they are preserved here for convenience
and insight into progression.

A recent one is [version 0.7](state-trans-v0.7).


